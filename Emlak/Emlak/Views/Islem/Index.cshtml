@model IEnumerable<Emlak.Models.Islem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IslemAd)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IslemAd)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IslemID }) |
            @Html.ActionLink("Details", "Details", new { id=item.IslemID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IslemID })
        </td>
    </tr>
}

</table>
