@model Emlak.Models.admin

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>admin</legend>

        @Html.HiddenFor(model => model.AdminID)

        <div class="editor-label">
            @Html.LabelFor(model => model.AdminKullanici)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.AdminKullanici)
            @Html.ValidationMessageFor(model => model.AdminKullanici)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.AdminSifre)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.AdminSifre)
            @Html.ValidationMessageFor(model => model.AdminSifre)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
