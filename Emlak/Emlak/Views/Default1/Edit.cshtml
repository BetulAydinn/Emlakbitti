@model Emlak.Models.IlanDısOzellik

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>IlanDısOzellik</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.IDOID)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.IDOID)
            @Html.ValidationMessageFor(model => model.IDOID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.IlanID, "Ilan")
        </div>
        <div class="editor-field">
            @Html.DropDownList("IlanID", String.Empty)
            @Html.ValidationMessageFor(model => model.IlanID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.DOID, "DısOzellik")
        </div>
        <div class="editor-field">
            @Html.DropDownList("DOID", String.Empty)
            @Html.ValidationMessageFor(model => model.DOID)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
