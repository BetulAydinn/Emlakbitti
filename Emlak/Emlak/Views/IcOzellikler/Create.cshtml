@model Emlak.Models.IlanIcOzellik

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>IlanIcOzellik</legend>


        <div class="editor-label">
            @Html.LabelFor(model => model.IOID, "IcOzellik")
        </div>
        <div class="editor-field">
            @Html.DropDownList("IOID", String.Empty)
            @Html.ValidationMessageFor(model => model.IOID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.IlanID, "Ilan")
        </div>
        <div class="editor-field">
            @Html.DropDownList("IlanID", String.Empty)
            @Html.ValidationMessageFor(model => model.IlanID)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
